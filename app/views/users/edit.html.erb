<div class="form-page">
  <div class="form-page__form">
    <h2>Éditer mon profil</h2>

    <%= form_with model: @user, url: user_path(@user), local: true, class: "form", id: "item-form", multipart: true do |f| %>
    
      <div class="form-group">
        <%= f.label 'Prénom', class: "form-label" %>
        <%= f.text_field :first_name, class: "form-control", autofocus: true, autocomplete: "first_name" %>
      </div>

      <div class="form-group">
        <%= f.label 'Nom', class: "form-label" %>
        <%= f.text_field :last_name, class: "form-control", autofocus: true, autocomplete: "last_name" %>
      </div>

      <div class="actions mb1">
        <%= f.submit "Enregistrer les changements", class:"btn white-btn bg-primary" %>
      </div>
    <% end %>

    <div class="form-page__form--links">
      <p class="mb1"><%= link_to 'Modifier mon e-mail ou mot de passe', edit_user_registration_path(@user) %></p>
      <p><%= link_to 'Retour', user_path(@user) %></p>
    </div>
  </div>
</div>