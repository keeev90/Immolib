<div class="container text-center">
  <h1 class="mb2">Étape 1/3 : sélectionnez un créneau de visite</h1>
  <p class="mb2">📅 Il y a actuellement <span class="bolder"><%= @property.available_slots %></span> <%= @property.available_slots > 1 ? "créneaux de visite disponibles" : "créneau de visite disponible" %></p>
  <p class="mb1">💡 Pour sélectionner un créneau de visite cliquez directement sur un des créneaux disponibles</p>
  <div id='calendar-wrapper'>
    <%= render partial:"appointments/candidate_calendar", locals: {property: @property, slots: @slots, date_arr: @date_arr, new_candidate: @new_candidate} %>
  </div>
  <% if !current_user %>
    <div class="slot-candidate-wrapper__calendar__title text-center"> 
      <p> Inscrivez-vous pour sélectionner un créneau </p>
      <%= link_to "M'inscrire", new_user_registration_path, class:"btn white-btn bg-primary" %>
    </div>
  <% end %>
  <% if current_user && @property.candidates.include?(current_user) %>
    <div class="center-btn mt2">
      <%= link_to "Valider et passer à l'étape suivante", step_2_property_path(@property), class: "btn white-btn bg-primary center-btn" %>
    </div>
  <% else %>
    <div class="center-btn mt2">
      <%= link_to "Merci de sélectionner un créneau", step_2_property_path(@property), class: "btn disabled center-btn"%>
    </div>
  <% end %>
</div>

