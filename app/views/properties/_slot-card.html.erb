<% if slot.available? %>
  <div class="property-wrapper__right__slot">
    <p> <%= slot.start_date.strftime("%H:%M") %> - <%= slot.end_date.strftime("%H:%M") %></p>
    <p> <%= slot.availability %> <%= slot.availability>1 ? "places" : "place" %> </p>
  </div>
<% else %>
  <div class="property-wrapper__right__slot full">
    <p> <%= slot.start_date.strftime("%H:%M") %> - <%= slot.end_date.strftime("%H:%M") %></p>
    <p> Ce créneau est complet. </p>
  </div>
<% end %>