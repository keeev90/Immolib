<%= link_to raw("<i class='fas fa-chevron-left'></i> Retour"), property_path(@property), class: "return-link pt1 pl4" %>
<div class="form-page">
  <div class="form-page__form">
    <h2>Éditer mon logement</h2>
    <p class="mb2 text-center"><em>Ces informations permettront à vos candidats de s’assurer qu’ils répondent bien à votre annonce en ligne</em></p>

    <%= form_with model: @property, url: property_path(@property), local: true, class: "form", id: "item-form", multipart: true do |f| %>
    
      <div class="field">
        <%= f.label 'Titre *' %><br />
        <p><small>💡 Uniformisez le titre avec celui de vos annonces en ligne (ex : PaP)</small></p>
        <%= f.text_field :title, autofocus: true, autocomplete: "title", required: true %>
      </div>

      <div class="field">
        <%= f.label 'Ville *' %>
        <p><small>💡 Précisez le code postal entre parenthèses</small></p>
        <%= f.text_field :city, autofocus: true, autocomplete: "city", required: true %>
      </div>

      <div class="field">
        <%= f.label 'Lien vers une annonce en ligne' %>
        <p><small><i class="fas fa-info"></i> Ce lien permet aux candidats d'accéder facilement aux détails de votre annonce</small></p>
        <%= f.text_field :other_link, autofocus: true, autocomplete: "other_link" %>
      </div>

      <div class="field">
        <%= f.label 'Instructions pour les visites' %>
        <p><small><i class="fas fa-info"></i> Ces instructions seront automatiquement communiquées aux candidats à l'approche d'une visite (ex : adresse, digicode, interphone, etc.)</small></p>
        <%= f.text_area :instructions, autofocus: true, autocomplete: "instructions" %>
      </div>

      <div class="actions mb1">
        <%= f.submit "Enregistrer les changements", class:"btn white-btn bg-primary" %>
      </div>
    <% end %>

  </div>
</div>