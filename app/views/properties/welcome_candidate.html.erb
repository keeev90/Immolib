<div class="welcome-page candidate">
  <h1 class="primary"> Bienvenue ! </h1>
  <div class="welcome_user-wrapper">
    <div class="welcome_user-wrapper__left">
      <p class="bolder primary">Rappel de l'annonce</p>
      <h4><%= @property.title%></h4>
      <small><p><%= @property.city%></p></small>
      <small class="mb2"><%= @property.other_link%></small>
      <%if @property.property_picture.attached?%>
        <%= image_tag @property.property_picture, alt: 'property picture', class: "house-picture pt2" %>
      <% end %>
    </div>
    <div class="welcome_user-wrapper__right">
      <p class="bolder primary">Bonjour</p>
      <% if @property.owner_project == "rent" %>
        <p><em>Je vous invite à sélectionner <strong>un créneau de visite</strong> disponible, à me transmettre <strong>votre dossier de location</strong>, 
          et à m’écrire quelques lignes <strong>à propos de vous et de votre projet</strong>.</em></p>
      <% else %>
        <p><em>Je vous invite à sélectionner un créneau de visite disponible 
        et à m’écrire quelques lignes à propos de vous et de votre projet.</em></p>
      <% end %>
      <p><em>A bientôt,</em></p>
      <%if @property.owner.first_name?%>
        <p><%= @property.owner.first_name %></p>
      <% else %>
        <p>Le propriétaire</p>
      <% end %>
      
      <% if user_signed_in? %>
      <%= link_to "C'est parti !", book_now_property_path(@property), class: "btn big-btn bg-primary white-btn" %>
        <% else %>
        <%= link_to "C'est parti !", book_property_path(@property), class: "btn big-btn bg-primary white-btn" %>
      <% end %>
      
      
    </div>
  </div>
</div>