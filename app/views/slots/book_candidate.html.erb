<div class="slot-candidate-wrapper">
  <div class="slot-candidate-wrapper__title pt1 section-title">
    <h1>Étape 2/3 : sélection d'un créneau disponible</h1>
    <h2>Sélectionnez un créneau de visite parmi les propositions disponibles.</h2>
    <p>Vous pourrez le modifier à tout moment depuis votre espace immolib. La disponibilité des créneaux est mis à jour automatiquement.</p>
  </div>
  <div class="slot-candidate-wrapper__calendar mt2">
    <div id='calendar-wrapper'>
      <%= render partial:"appointments/candidate_calendar", locals: {property: @property, slots: @slots, date_arr: @date_arr} %>
    </div>

    <% if !current_user %>
      <div class="slot-candidate-wrapper__calendar__title text-center"> 
        <p> Inscrivez-vous pour sélectionner un créneau. </p>
        <%= link_to "M'inscrire", new_user_registration_path, class:"btn white-btn bg-primary" %>
      </div>
    <% end %>

    <% if current_user && @property.candidates.include?(current_user) %>
      <div class="center-btn">
        <%= link_to "Valider et passer à l'étape suivante", send_message_property_path(@property), class: "btn white-btn bg-primary center-btn" %>
      </div>
    <% else %>
      <div class="center-btn">
        <%= link_to "Merci de sélectionner un créneau", send_message_property_path(@property), class: "btn disabled center-btn"%>
      </div>
    <% end %>
  </div>
</div>
